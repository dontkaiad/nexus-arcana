# Telegram + async
aiogram==3.13.1
aiohttp==3.10.11
aiosqlite==0.22.1
aiofiles==23.2.1

# LLM / HTTP
anthropic==0.40.0
httpx==0.27.2

# RAG (Voyage AI embeddings + pgvector):
# voyageai — нужна свежая, иначе не знает модель voyage-4-lite.
# Векторный стор — pgvector в том же Postgres (см. SQLAlchemy/psycopg2 ниже);
# python-пакет pgvector НЕ нужен — core/rag.py передаёт vector через CAST в SQL.
voyageai>=0.3.2

# Postgres / ORM / миграции
SQLAlchemy==2.0.48
psycopg2-binary==2.9.12
alembic==1.16.5

# Mini App backend (FastAPI)
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.20
jinja2==3.1.4
itsdangerous==2.2.0

# Валидация / планировщик / env / tz
pydantic==2.9.2
APScheduler==3.11.2
python-dotenv==1.0.1
tzlocal==5.3.1

# Остаточный импорт после миграции (вычистить из кода отдельно)
notion-client==2.2.1
